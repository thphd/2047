<div class="pagination">

  {%for group in pagination.button_groups%}
  <div class="group">
    {%for i in group%}
    <a href="{{i[1]}}">
      <div class="pagenumber {{' current' if i[2] else ''}}">{{i[0]}}</div>
    </a>

    {%endfor%}
  </div>
  {%endfor%}

</div>
